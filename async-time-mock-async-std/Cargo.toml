[package]
name = "async-time-mock-async-std"
description = "Mockable time for use in async runtimes (async-std compatibility)."
version = "0.0.1"
edition = "2021"
license = "MIT"
authors = [
	"Max Bruckner",
	"Thomas Keppler",
]

[dependencies]
async-std = "1"
async-time-mock-core = {version = "0.0.1", path = "../async-time-mock-core"}
